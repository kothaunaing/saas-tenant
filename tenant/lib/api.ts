// Barrel re-export preserving backward compatibility for existing imports.
// Domain-specific endpoints are separated under ./api/ (customers, services, staff, appointments, rewards, auth, workspace).
export * from "./api/index";
